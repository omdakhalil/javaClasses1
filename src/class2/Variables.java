package class2;

public class Variables {
    public static void main(String[] args) {
        /*
        below line is reserving a box in the computers memory
        we are colling it box1 if we need the information that we are storing inside this box
        we can simple say the computer give us the contens of box1.
        int= what type of data we want to store.
        box1= is the name of the box.
        = sing will take whatever we write after it and will store it inside the box1
        10= is what we are storing the box
         */
        int box1=10;
       // System.out.println(box1);
        // bring the contents of box1 and print them on the console
        // to store whole numbers we have 4 different of boxes.
        byte box2=127; // range for byte is from -128 to 127 if we need to store larger numbers we should try short
        byte box3=-128;
        short box4=32767;
        int biggerBox=123456484; // most common type of box
        long maxBox=151111556465454l;
        long box12=128555l;
        short box5=-32766;
       // System.out.println(box12);
        short myBox=10000;
        System.out.println(box2);





    }
}
